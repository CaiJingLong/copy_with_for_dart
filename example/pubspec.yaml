name: example
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.9.3 <3.0.0'

dependencies:
  copy_with_gen: 
    path: ../

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
